# ------------------------------------------------------------------------------------------------------------------------
# Pipeline Template to build and deploy Azure Resources and Application
# ------------------------------------------------------------------------------------------------------------------------
parameters:
- name: environments
  type: object
  default: ['DEV']
- name: singleEnvironment
  default: 'false'
- name: deployBicep
  default: false
- name: parseBicepOutputs
  default: false
- name: runUnitTests
  default: 'false'
- name: runSmokeTests
  default: 'false'
- name: runGHASScan
  default: 'false'
- name: runMSDevSecOpsScan
  default: 'false'

# ----------------------------------------------------------------------------------------------------
stages:
- stage: ScanApplication
  displayName: Scan Application
  jobs:
  - ${{ if or(eq(lower(parameters.runMSDevSecOpsScan), 'true'), eq(lower(parameters.runGHASScan), 'true')) }}:
    - template: templates/scan-code-template.yml
      parameters:
        environmentName: 'DEV'
        runMSDevSecOpsScan: ${{ parameters.runMSDevSecOpsScan }}
        runGHASScan: ${{ parameters.runGHASScan }}
  - ${{ else }}:
    - job: ScanSkippedJob
      displayName: Skipping Scan

# ----------------------------------------------------------------------------------------------------
- stage: BuildApp
  displayName: Build Application
  dependsOn: ScanApplication
  jobs:
  - template: templates/function-build.yml
    parameters:
      environmentName: 'DEV'
      runUnitTests: ${{ parameters.runUnitTests }}

# ----------------------------------------------------------------------------------------------------
- ${{ each environmentName in parameters.environments }} :
  - stage: CreateInfra${{ environmentName }}
    displayName: ${{ environmentName }} Infrastructure
    dependsOn: BuildApp
    # Condition is deployBicep is true AND (build complete and a single environment is being deployed, 
    # or the environment is 'dev', or if qa/prod, the previous stage (dev->qa or qa->prod) succeeded)
    condition: and(eq(${{ parameters.deployBicep }}, true), or(eq(${{ parameters.singleEnvironment }}, 'true'), and(eq(upper('${{ environmentName }}'), 'DEV'), succeeded('BuildApp')), and(eq(upper('${{ environmentName }}'), 'QA'), succeeded('DeployAppDEV')), and(eq(upper('${{ environmentName }}'), 'PROD'), succeeded('DeployAppQA'))))
    jobs:
    - template: templates/bicep-deploy-template.yml
      parameters:
        environmentName: ${{ environmentName }}
        parseBicepOutputs: ${{ parameters.parseBicepOutputs }}

  - stage: DeployApp${{ environmentName }}
    displayName: Deploy ${{ environmentName }} App
    dependsOn: CreateInfra${{ environmentName }}
    # Run if infrastructure deployment succeeded (when deployBicep=true) OR if deployBicep=false (skip infra)
    condition: or(and(succeeded('CreateInfra${{ environmentName }}'), eq(${{ parameters.deployBicep }}, true)), eq(${{ parameters.deployBicep }}, false))
    jobs:
    - template: templates/function-deploy.yml
      parameters:
        environmentName: ${{ environmentName }}

  - ${{ if eq(lower(parameters.runSmokeTests), 'true') }}:
    - stage: SmokeTest${{ environmentName }}App
      displayName: Smoke Test ${{ environmentName }}
      dependsOn: DeployApp${{ environmentName }}
      condition: succeeded('DeployApp${{ environmentName }}')
      variables:
        - template: ../vars/var-common.yml
      jobs:
      - template: templates/playwright-template.yml
        parameters:
          environment: ${{ environmentName }}
          testType: '${{ variables.playwrightAPITestType }}'
          testPath: ${{ variables.playwrightAPITestPath }}
          debug: true
